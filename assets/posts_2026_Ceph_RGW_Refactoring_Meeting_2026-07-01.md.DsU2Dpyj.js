import{_ as t,o as a,c as i,a2 as n}from"./chunks/framework.BXA5Pi9t.js";const u=JSON.parse('{"title":"Ceph RGW Refactoring Meeting 2026-07-01","description":"","frontmatter":{"title":"Ceph RGW Refactoring Meeting 2026-07-01","outline":"deep"},"headers":[],"relativePath":"posts/2026/Ceph_RGW_Refactoring_Meeting_2026-07-01.md","filePath":"posts/2026/Ceph_RGW_Refactoring_Meeting_2026-07-01.md","lastUpdated":null}'),r={name:"posts/2026/Ceph_RGW_Refactoring_Meeting_2026-07-01.md"};function o(l,e,c,s,d,h){return a(),i("div",null,[...e[0]||(e[0]=[n('<p>I&#39;m organizing the meeting summary in Chinese while keeping technical terms in English, and selecting the most relevant tags from the available options like RGW, RADOS, performance optimization, RocksDB, and testing.--- title: Ceph RGW Refactoring Meeting 2026-07-01 date: 2026-07-10 updated: 2026-07-11 tags:</p><ul><li>RGW</li><li>RADOS</li><li>性能优化</li><li>RocksDB</li><li>会议纪要 categories:</li><li>视频总结 subtitle: Ceph_RGW_Refactoring_Meeting_2026-07-01</li></ul><hr><h1 id="ceph-rgw-refactoring-meeting-会议纪要-2026-07-01" tabindex="-1">Ceph RGW Refactoring Meeting 会议纪要（2026-07-01） <a class="header-anchor" href="#ceph-rgw-refactoring-meeting-会议纪要-2026-07-01" aria-label="Permalink to &quot;Ceph RGW Refactoring Meeting 会议纪要（2026-07-01）&quot;">​</a></h1><p>本次会议围绕 RGW 在多站点（multi-site）环境下的陈旧 bucket instance 清理、Rust 测试框架的引入、RGW 重载期间的 race condition 修复，以及 core/RADOS 团队如何提升小对象性能等主题展开。以下是各议题的详细纪要。</p><h2 id="一、多站点环境下陈旧-bucket-instance-累积问题" tabindex="-1">一、多站点环境下陈旧 bucket instance 累积问题 <a class="header-anchor" href="#一、多站点环境下陈旧-bucket-instance-累积问题" aria-label="Permalink to &quot;一、多站点环境下陈旧 bucket instance 累积问题&quot;">​</a></h2><h3 id="问题背景" tabindex="-1">问题背景 <a class="header-anchor" href="#问题背景" aria-label="Permalink to &quot;问题背景&quot;">​</a></h3><p>在 Squid 集群中观察到大量陈旧 bucket instance 持续累积。所有集群均为 multi-site 部署，这是一个已知问题。最初是客户在查询 replication status 时暴露出来的：客户对某个对象做 head object 时，希望看到复制状态为 replicated 或 completed，但即便对象在几周前就已完成复制，状态仍长期停留在 pending。</p><p>根因在于 replication status 的上报依赖于 bucket index log 的 trimming（blog trimming）机制。当客户 head 一个对象时，RGW 会将该对象的修改时间与该 bucket log 中最旧的 entry 做比较。如果 bucket log 没有被 trim，旧 entry 依然存在，RGW 就会误判该对象尚未复制，给出错误的 pending 状态。因此 blog trimming 对于准确上报 replication status 至关重要。</p><h3 id="累积规模与自动-trimming-失效" tabindex="-1">累积规模与自动 trimming 失效 <a class="header-anchor" href="#累积规模与自动-trimming-失效" aria-label="Permalink to &quot;累积规模与自动 trimming 失效&quot;">​</a></h3><p>问题的核心在于：在 Squid 及更早版本的 multi-site 部署中，删除 bucket 时并不会删除对应的 bucket instance 和 index object，这些残留 instance 会不断堆积。以生产环境为例，某个集群仅有约 2 万个存活 bucket，但由于过去数年的删除操作，累积了约 200 万个 instance。</p><p>这使得自动 blog trimming 几乎失效：默认配置下，trimming 线程每 20 分钟启动一次，默认挑选 16 个 bucket，其中仅 4 个是从 bucket instance 列表中扫描处理。面对 200 万个 instance，每 20 分钟只处理 4 个，根本无法在合理时间内完成。即便调高默认参数，帮助也有限。</p><h3 id="清理方案讨论" tabindex="-1">清理方案讨论 <a class="header-anchor" href="#清理方案讨论" aria-label="Permalink to &quot;清理方案讨论&quot;">​</a></h3><ul><li>Tentacle 与 main 分支已有修复（对应九年前的 tracker，涉及 clean up deleted buckets），但该修复复杂且高风险，至今仍存在由此引入的测试用例失败，团队尚未完全理解其正确的设计，因此<strong>不建议进一步 backport 到 Squid</strong>（内部亦然）。</li><li>即使 backport 了修复，也无法清除已存在的 200 万个残留 entry，仍需额外的清理手段。</li><li><code>radosgw-admin reshard stale-instances list / rm</code> 工具可用于列出并删除陈旧 instance，但官方文档明确不建议在 multi-site 环境使用。</li></ul><p><strong>风险分析：</strong> multi-site 可能仍在对这些已 unlink 的 instance 处理对象删除。存在竞态场景——主站点删除了 bucket 内所有对象并删除 bucket，而 secondary 仍需处理这些删除的复制。若此时 metadata 被删除，secondary 将无法完成处理，可能导致对象成为孤儿（orphan）。特别是丢失 data log entry 的情况下，可能永远丢失对某个 bucket 的追踪而无法完成同步。</p><p><strong>达成的建议做法：</strong> 通过 <code>reshard stale-instances list</code> 列出的应当仅是已删除的 bucket。对这些 instance 逐个执行 <code>bucket sync status</code> 确认所有 zone 均已 caught up 后，再在主站点上手动删除。这样既避免了直接批量 <code>rm</code> 的竞态风险，也能在受控范围内清理残留。会议同意此路径为当前最佳选择，并由提出者在自有环境中实测该场景下 multi-site 的行为（例如 secondary 遇到 no entry 错误时是优雅处理还是无限重试）。</p><h3 id="替代思路-replication-notifications" tabindex="-1">替代思路：Replication Notifications <a class="header-anchor" href="#替代思路-replication-notifications" aria-label="Permalink to &quot;替代思路：Replication Notifications&quot;">​</a></h3><p>与会者提出，与其让客户端轮询 replication status attribute（该属性有 replicated / completed / pending / failed 四种状态），不如使用 replication notifications 推送机制。客户端配置 Kafka 等消息通道后，对象完成同步时即可收到 notification，无需持续轮询。由于当前 replication status 因 blog auto-trim 严重滞后而不可靠，notification 已作为团队推荐给客户的替代方案。</p><h2 id="二、引入-rust-测试框架的讨论" tabindex="-1">二、引入 Rust 测试框架的讨论 <a class="header-anchor" href="#二、引入-rust-测试框架的讨论" aria-label="Permalink to &quot;二、引入 Rust 测试框架的讨论&quot;">​</a></h2><p>Adam 计划将原本准备集成进 S3 tests 的一批 Python 测试，改写为一个独立的 Rust starter 测试，先在 Teuthology 上试验其可行性，积累经验后再决定后续方向。</p><p>主要争论与共识如下：</p><ul><li><strong>不维护两套测试代码库</strong>：反对同时维护 Python 与 Rust 两套重复测试。共识是采取「按需新增用 Rust 写、或在有动力时逐步用 Rust 替换 Python」的渐进策略，先取一小部分用 Rust 替换 Python，避免双语言重复维护，同时也让 PR 规模可控（无论 AI 工具多好，每行代码仍需人工 review，超大 PR 难以审查）。</li><li><strong>编译与打包方式</strong>：目前倾向于直接在 Teuthology 上通过 cargo 构建。长期看应像其他 Teuthology 测试一样构建 package（可用 CMake 调用 cargo 构建 s3-tests-rs，随 ceph test 包一起发布）。</li><li><strong>保留独立测试分支的价值</strong>：将测试代码与被测分支分离非常有用，开发者可直接推 Python 代码并立即运行，无需等待 Shaman 构建数小时甚至数天。</li><li><strong>反对立即打包的理由</strong>：越来越多的原因（安全考量）促使团队不希望构建/测试节点从公网随意抓取内容，未来 cargo 从公网拉取 crates 并在 Teuthology 节点上构建可能被禁止。这反而成为保留 Python 的一个有力论据。同时，现有 Java S3 test suite 每次运行都从公网拉取大量 Java 依赖，因此当前不必急于为 Rust 打包。</li></ul><p><strong>关于性能收益</strong>：与会者认为语言本身并非提速关键，机器 setup、测试中的 polling/sleep 都不会因语言改变。真正能显著提速的是测试并行执行，这取决于测试框架而非语言（pytest 已有并行插件可用）。多数人表示本地只跑小子集验证，运行时间并非主要顾虑，性能不是引入 Rust 的主要动机。</p><p><strong>引入 Rust 的真正动机</strong>：Rust 真正遵循 semantic versioning，而 Python 在版本管理（尤其是 minor version 与 Boto 版本）上长期造成困扰——RGW 团队早已知道这是一颗定时炸弹。此外获得主流 AWS SDK 的测试覆盖也很有价值。</p><p><strong>基础设施升级流程改进</strong>：会议提出应采用「先 staging、提前发现问题」的正向流程，而非等升级后测试全部失败再补救。目前针对 Rocky 10，dashboard/management 团队（受 Python breakage 影响更大）已在提前做 staging 与测试构建，预计 Rocky 11 也将沿用此流程。团队一致认为不应因 Python QA 的维护痛点而阻碍基础设施保持系统更新，策略应是「增量新增而非丢弃现有覆盖」，保留有价值且广泛使用的 Python + Boto 3 测试覆盖。</p><p><strong>缺失的 SDK 覆盖</strong>：目前缺少 JavaScript/TypeScript SDK（使用广泛）与 Golang SDK 的测试覆盖，可考虑补充。C++（AWS C++ SDK 体积庞大、无 OS 打包、需从源码构建）使用较少，暂可不做。</p><h2 id="三、rgw-重载期间的-race-condition-与-finalize-shutdown" tabindex="-1">三、RGW 重载期间的 race condition 与 finalize/shutdown <a class="header-anchor" href="#三、rgw-重载期间的-race-condition-与-finalize-shutdown" aria-label="Permalink to &quot;三、RGW 重载期间的 race condition 与 finalize/shutdown&quot;">​</a></h2><p>Adam 已从当前 PR 中移除了 finalize 相关代码，配合 object change 等改动，实践中可能无需 finalize 即可工作。但他在架构层面强烈认为，长期而言需要类似 <code>finalize</code> 的机制，或类似为 data log 编写的 <code>cancel + shutdown</code> 组合——若希望 IO context 在多次 reload 间持续运行，后者是更合适的方案。</p><p>问题现象疑似为：data log shard completion 在 data log 被 deallocate 之后触发。但由于当时大量使用 strands，也可能是操作未运行在预期的 strand 上所致，这正是移除 finalize 在实践中可能仍有效的原因。不过他仍相信在缺少 finalize 或 cancel-and-shutdown 的情况下，reload 时仍存在潜在的 race condition，只是未必会被触发。</p><p><strong>后续行动</strong>：先移除 finalize、合并现有 crash 修复；随后针对 data log restore 场景实现基于 finalize + shutdown 的方案（相关代码已写好）；并 review Matt Healer 的 restore 分支，在其基础上工作，优先处理当前 crash 修复，再解决更大的问题。</p><h2 id="四、core-rados-团队如何提升-小-对象性能-alex" tabindex="-1">四、Core/RADOS 团队如何提升（小）对象性能（Alex） <a class="header-anchor" href="#四、core-rados-团队如何提升-小-对象性能-alex" aria-label="Permalink to &quot;四、Core/RADOS 团队如何提升（小）对象性能（Alex）&quot;">​</a></h2><p>背景：Ceph London 的讨论最初聚焦于提升小对象性能，进而演变为「究竟哪些性能环节不满意」。团队曾设想通过 object packing（将大量小对象打包成大对象）来提升空间效率与 backfill 性能，但在设计阶段发现该方案并不能达成目标，好在未写代码前就发现了。core 团队近期已显著扩员，正寻找对 RGW 有价值的项目。</p><h3 id="核心痛点-omap-与-large-omap-warning" tabindex="-1">核心痛点：OMAP 与 large OMAP warning <a class="header-anchor" href="#核心痛点-omap-与-large-omap-warning" aria-label="Permalink to &quot;核心痛点：OMAP 与 large OMAP warning&quot;">​</a></h3><p>RGW 团队认为，来自 core 的最大性能问题是 OMAP。任何能提升 OMAP、data、以及 key-value store 性能的改进都有意义。RGW 团队长期主张 large OMAP warning 是 OSD 应修复的 bug，而非 RGW 的 bug——large OMAP warning 对应用而言像是不合理的约束，因为 OMAP 本质就是 key-value 接口。</p><p>Alex 表示需要进一步理解 OMAP 大小限制的原因，认为单个大 map 与分散在多个对象上的多个小 map 对 OSD 的负担未必更重。限制可能源于历史原因（早期 recovery 需在一次锁内处理全部 OMAP key），但如今 recovery 已改为分批（batch）进行 OMAP backfill，该约束已不成立。当前的限制更像是鼓励客户端分散负载而非集中。</p><h3 id="规模数字" tabindex="-1">规模数字 <a class="header-anchor" href="#规模数字" aria-label="Permalink to &quot;规模数字&quot;">​</a></h3><ul><li>动态 resharding 自动最多到 1999 个 shard，管理员可手动更高。</li><li>shard 数与 index pool 的 PG count 相关：若 shard 数超过 PG count，多个 shard 会堆在同一 PG 上，得不到更多 write 并行度。</li><li>参考量级：约 2000（1999）个 shard 上分布约 1000 亿（100 billion）个 key/对象。在此之上进一步 sharding 只增加 listing 的开销，而不再带来写并行收益。</li></ul><h3 id="小对象写入优化-并行化-bucket-index-事务" tabindex="-1">小对象写入优化：并行化 bucket index 事务 <a class="header-anchor" href="#小对象写入优化-并行化-bucket-index-事务" aria-label="Permalink to &quot;小对象写入优化：并行化 bucket index 事务&quot;">​</a></h3><p>小对象上传的主要延迟来自 bucket index 事务，即通过 CLS RGW 对 OMAP 的写入。事务流程为：先在 OMAP 中 prepare op → 写 data object 到 RADOS → 再在 index 上 complete。</p><p>关键观察：</p><ul><li>complete 阶段不仅更新 bucket index entry（这部分对不同对象是相互独立的），还要更新 bucket stats，而所有 complete 都会在 bucket stats 上产生争用（contend）。</li><li>prepare op 相信只是写入单个 key。若能让不同（无关）对象的 OMAP 操作在<strong>单个 shard 内</strong>也并行、互不阻塞，将带来显著提升——尽管尚不确定 OSD 层面是否可行。合并（coalesce）多个 OMAP entry 被认为可行且能带来性能提升。</li><li>从 RGW 视角，prepare 与 head object 写入是同步的，而 complete 是异步的（可在其完成前先响应客户端）。因此即便 complete 较昂贵，<strong>加速 prepare</strong> 就能带来很大收益。</li></ul><p><strong>行动</strong>：Alex 将主要与 Bill Scales 讨论并行化方案，以及将大量更新合并进单条消息以降低复制开销（写入路径在某些场景已有此做法）。结论是关键在于<strong>写路径</strong>而非读路径。</p><h3 id="读路径-bucket-listing-的算法问题" tabindex="-1">读路径：bucket listing 的算法问题 <a class="header-anchor" href="#读路径-bucket-listing-的算法问题" aria-label="Permalink to &quot;读路径：bucket listing 的算法问题&quot;">​</a></h3><p>读的主要扩展瓶颈在 bucket listing，且属于算法问题：由于 shard 之间没有顺序（order），每次 listing 请求都必须读取每个 shard。Eric 正在开发一种 ordered indexing 策略，使 shard 之间具备顺序，这是客户端侧的算法改进，不依赖 OSD 变快。是否在 OSD 内实现类似 Foundation DB 的分布式 ordering 尚有争议。</p><h3 id="kv-store-设计设想-rgw-与存储解耦" tabindex="-1">KV store 设计设想（RGW 与存储解耦） <a class="header-anchor" href="#kv-store-设计设想-rgw-与存储解耦" aria-label="Permalink to &quot;KV store 设计设想（RGW 与存储解耦）&quot;">​</a></h3><p>有人（Matt 支持）在探讨在 RGW 下引入 KV store 的早期设想，动机是让 RGW 独立于底层存储。举例：Spark 工作负载中目录名嵌在 S3 object key 里，若 S3 object 直接映射到 RADOS object，重命名一个目录就等于对上千个对象做 copy + delete；而在 KV store 中，只需 copy + delete 上千个 key，数据本身无需移动。这说明 S3 与 RADOS object 之间存在过强的耦合，理想中底层应是 data store 而非 S3 object store。</p><p>会议倾向于<strong>限定当前讨论范围</strong>：先把可落地的性能改进做出来（对所有场景都是全面收益），比构建类 Foundation DB 的长期架构更实际，后者对 core 团队投入巨大且未必是正确方向。需要 Eric（该方向负责人）参与后续讨论。</p><h3 id="rocksdb-相关" tabindex="-1">RocksDB 相关 <a class="header-anchor" href="#rocksdb-相关" aria-label="Permalink to &quot;RocksDB 相关&quot;">​</a></h3><ul><li>有人希望挑战「RocksDB 的问题无能为力」这一假设，认为仍有提升空间，近期已在调查。</li><li>也提出希望升级到不会在 GCC 16 下 break 的较新 RocksDB 版本（Alex 请将该 comment 通过 Slack 发给他以便消化）。</li></ul><h3 id="缓存控制-cache-hint" tabindex="-1">缓存控制（cache hint） <a class="header-anchor" href="#缓存控制-cache-hint" aria-label="Permalink to &quot;缓存控制（cache hint）&quot;">​</a></h3><p>Gabby 提出一个小改进：bucket listing 时避免 RocksDB 做（读）缓存。更广义地说，RADOS 未暴露完整 API：读对象（如为 DD 读数据）时无法告诉底层「不要污染 cache」，listing 或读 OMAP 时也希望这些内容不进 cache，但目前无法控制。</p><ul><li>可分两层实现 cache hint：RocksDB 内部有读取时「不进 cache」的 flag；OSD 层面也可设置类似 <code>rados cache</code>/<code>osd cache</code> 的提示。</li><li>对 scan workload 而言缓存通常有害，但需注意：现有的 unordered sharding 方案（尤其在 shard 众多的 bucket 上）可能会反复 fetch 相同 entry（因为每次只从每个 shard 消费极少量 entry 来产生结果），因此对大 bucket 而言缓存可能仍有帮助。</li><li>结论：理想做法是把 cache 控制加入 API，由代码按场景自行决定何时启用/禁用缓存（例如对小 bucket 或 Eric 的 ordered/unordered indexing listing 禁用缓存）。</li></ul><h2 id="五、cmpmap-增强与-life-cycle-处理改为-per-shard-een" tabindex="-1">五、CmpMap 增强与 Life Cycle 处理改为 per-shard（Een） <a class="header-anchor" href="#五、cmpmap-增强与-life-cycle-处理改为-per-shard-een" aria-label="Permalink to &quot;五、CmpMap 增强与 Life Cycle 处理改为 per-shard（Een）&quot;">​</a></h2><p><strong>议题 1：CmpMap（compare map）增强</strong>：一个改进 CmpMap 的 PR，使其更适用于 read-modify-write 场景，可在写入时确保 value 未被更改。原实现对已有用例可用，但对更通用的场景存在问题，该 PR 予以修正。</p><p><strong>议题 2：Life Cycle 处理从 per-object 改为 per-shard co-routine</strong>：当前生命周期策略评估与删除是每个对象一个 co-routine，提议改为每个 shard 一个 co-routine。</p><p>好处：</p><ul><li>可消除 ordered 与 unordered listing 的区分——listing 在单个 co-routine 内完成、遍历单个 shard，而由于 listing 已在所有 shard 间并行，对象处理天然被分散，后续删除不会落在相同 co-routine、也不会触碰相同 index。</li><li>每个 co-routine 处理不同 shard，写入之间不会自我争用（contend）。</li></ul><p>设计挑战：</p><ul><li>需处理与 resharding 的竞态。当前做法是 reshard 发生时因 object ID 改变而失败，相当于跳过当天、次日再处理；若完全不监控 resharding，行为与现状一致。遍历超大 bucket 耗时很长，需防范 resharding 带来的问题。</li><li>需考虑与 RADOS 之外后端的交互：sharding 是 RADOS 的细节，其他后端可能没有 shard，届时单个 co-routine 可能限制其处理能力。需确认其他后端（如 POSIX）是否支持 life cycle——只要后端实现了 LC list 相关接口即应可用；若 POSIX 尚未支持，Matt 与 Dan 应会需要它。</li></ul><p>提出者已完成原型，除 per-shard 处理外，还合并了常规对象与 multipart 对象的处理行为，使两者无需分开做 listing、行为基本一致。对无 shard 的后端将回退到原行为，具体如何调和两种方案仍需进一步研究。欢迎在 issue ticket 上补充反馈，Matt 也将收到相关链接。</p><h2 id="后续行动清单" tabindex="-1">后续行动清单 <a class="header-anchor" href="#后续行动清单" aria-label="Permalink to &quot;后续行动清单&quot;">​</a></h2><ul><li>陈旧 instance 清理：采用 <code>reshard stale-instances list</code> + 逐个 <code>bucket sync status</code> 确认 + 主站点手动删除；提出者在自有环境实测 multi-site 在删除竞态下的行为。</li><li>不再向 Squid backport clean-up-deleted-buckets 修复（内部亦不做）；向客户推荐用 replication notifications 替代 replication status 轮询。</li><li>Rust 测试：Adam 先写独立 Rust starter 测试在 Teuthology 试验，采取渐进替换/新增策略；暂不打包；保留 Python + Boto 3 覆盖；考虑补充 JavaScript/TypeScript 与 Golang SDK 覆盖。</li><li>RGW reload：先合并现有 crash 修复（移除 finalize），再实现 data log 的 finalize + shutdown 方案，并 review Matt Healer 的 restore 分支。</li><li>性能：core 团队评估 OMAP / large OMAP warning 约束，重点推进写路径（prepare op）并行化与 OMAP entry 合并；Alex 与 Bill Scales 深入讨论；升级 RocksDB（GCC 16 兼容）；推动为 RADOS 增加 cache hint API。</li><li>Life Cycle per-shard 改造：Een 继续完善原型并在 issue ticket 收集反馈。</li></ul>',62)])])}const g=t(r,[["render",o]]);export{u as __pageData,g as default};
