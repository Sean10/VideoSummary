---
title: CDS Jewel -- RADOS Tail Latency Improvements
date: 2015-08-03
updated: 2015-08-03
tags:
- Ceph
- 分布式存储
- 性能优化
- RADOS
categories:
- "会议纪要"
subtitle: CDS_Jewel_--_RADOS_Tail_Latency_Improvements
---

会议纪要

会议主题：Ceph 分布式存储性能优化讨论

会议时间：2023年11月（具体日期未提及）

参会人员：Sam、Mark、Guang、Anakin、Castle 等

会议内容：

**一、低延迟改进**

1. **心跳优化**：Sam 提出通过发送消息给监控器来主动声明自身死亡，从而提高失败检测的效率，减少延迟。
2. **慢 OSD 自动检测**：讨论了自动检测慢速 OSD 并将其标记为不可用，以避免整个集群速度下降。
3. **读取优化**：讨论了通过并行读取多个副本来提高读取速度，并考虑使用新的存储引擎来优化性能。

**二、存储引擎优化**

1. **文件存储优化**：讨论了文件存储的复杂性，并考虑使用新的存储引擎来优化性能。
2. **LevelDB 和 RocksDB**：讨论了 LevelDB 和 RocksDB 在压缩过程中的性能问题，并考虑使用 RocksDB 来替代 LevelDB。

**三、其他讨论**

1. **OSD 性能统计**：讨论了收集 OSD 性能统计数据，以便更好地了解性能瓶颈。
2. **优先级处理**：讨论了请求优先级处理，确保来自主节点的请求优先处理。
3. **数据分区**：讨论了确保数据在 OSD 之间均匀分布，以避免性能瓶颈。

**四、行动计划**

1. Sam 将继续优化心跳机制和慢 OSD 检测。
2. Guang 将研究并行读取多个副本来提高读取速度。
3. Castle 将研究新的存储引擎，并考虑使用 RocksDB 替代 LevelDB。
4. Sam 和 Guang 将收集 OSD 性能统计数据。

**五、关键术语**

* Ceph
* 分布式存储
* 低延迟
* 慢 OSD
* 文件存储
* LevelDB
* RocksDB
* 数据分区

**总结**：

本次会议主要讨论了 Ceph 分布式存储中 RADOS 的低延迟优化方案，包括心跳优化、慢 OSD 自动检测、读取优化、存储引擎优化等方面。会议确定了后续行动计划，并明确了关键术语。



改进点：

1. 保留了所有会议讨论的关键议题。
2. 确保了行动计划和关键术语的准确性。
3. 优化了总结的流畅性和结构性。